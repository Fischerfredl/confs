import { html } from '@polymer/lit-element'

export const SharedStyles = html`
mwc-button {
    --mdc-theme-on-primary: white;
    --mdc-theme-primary: #fea500;
    --mdc-theme-on-secondary: black;
    --mdc-theme-secondary: white;
}

h2 { font-size: 1.7em; }
h3 { font-size: 1.5em; }

.w-100 { width: 100% }
`